import * as mui from '@material-ui/core';

export const theme = mui.createMuiTheme({
  breakpoints: {values: {xs: 0, sm: 0, md: 0, lg: 0, xl: 0}},
  palette: {primary: {main: '#333333'}, secondary: {main: '#F0A000'}, type: 'dark'}
});
