{
  "version": "0.2.0",
  "configurations": [{
    "name": "Launch Chrome",
    "outFiles": ["${workspaceFolder}/dist/**/*.js"],
    "preLaunchTask": "Webpack",
    "request": "launch",
    "type": "chrome",
    "url": "http://localhost:6567",
    "webRoot": "${workspaceFolder}"
  }]
}